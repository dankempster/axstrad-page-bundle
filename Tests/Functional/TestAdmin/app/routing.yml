admin:
    resource: '@SonataAdminBundle/Resources/config/routing/sonata_admin.xml'
    prefix: /admin

_sonata_admin:
    resource: .
    type: sonata_admin
    prefix: /admin

# Catch all route, so should always be last
axstrad_page:
    resource: '@AxstradPageBundle/Resources/config/routing.yml'
